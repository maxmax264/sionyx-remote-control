{
  "name": "sionyx-remote-control",
  "version": "1.0.0",
  "private": true,
  "description": "MeshCentral server for SIONYX remote control - stores state in MongoDB Atlas so it survives Render restarts, since Render's free tier has no persistent disk.",
  "scripts": {
    "start": "node start.js"
  },
  "dependencies": {
    "meshcentral": "^1.2.5",
    "mongodb": "^6"
  },
  "engines": {
    "node": ">=18"
  }
}